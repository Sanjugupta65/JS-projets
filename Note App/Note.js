const Title = document.getElementById(note-title);
const noteItem = document.getElementById(note-body)
const saved_things = document.getElementById(save-btn)
const saved_notes = document.getElementById(note-container)

saved_things.addEventListener('click',function(){
    var titlevalue = saved_things.value
})